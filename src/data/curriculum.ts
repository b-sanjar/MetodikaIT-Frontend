// Static UI constants for the informatics curriculum.
// Lesson content itself lives on the backend (see API_ENDPOINTS.md).

export const GRADES = Array.from({ length: 11 }, (_, i) => i + 1)

export const QUARTER_NAMES = ['I chorak', 'II chorak', 'III chorak', 'IV chorak']
